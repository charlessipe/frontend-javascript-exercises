module.exports.getKeys = function(anObject) {  //accept one argument, an object
  for(var property in anObject) {  
    return (anObject[property]);  //return an array of each of the keys in the object
  }  
};


module.exports.getValues = function(myObject){  //accept one argument, an object
  // return an array of each of the values in the object
  //getValues({name: "Rob", age: 101}) // returns ["Rob", 101]
};  



module.exports.objectToArray = function(){
  //convert the object passed in to an array of strings
  key " is " value //each string should be in the format of "[key] is [value]" for each key/value pair in the object
  //if an empty object is passed in, an empty array should be returned
  
};




